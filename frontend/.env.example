# URL of the deployed backend API. Leave unset for local dev (defaults to
# http://127.0.0.1:8000). In production set this in your host's env vars, e.g.:
#   VITE_API_BASE=https://your-backend.onrender.com
VITE_API_BASE=
