# Python
.venv/
__pycache__/
*.pyc

# Feature extraction cache (regenerated by train.py)
artifacts/feature_cache.npz

# Keep the trained model + metadata committed so deploys don't retrain:
#   artifacts/model.joblib
#   artifacts/meta.json
# Keep the pose model committed so the container doesn't need to download it:
#   pose_model/pose_landmarker_full.task
